main() {
  // Para fazer uma concatenação de texto, é preciso utilizar o +
  var text = "Hello " + "World";
  print(text);

  // Concatenação de texto com variáveis
  var name = "Vinícius ";
  var surname = "Gabriel";
  var fullName = name + surname;
  print(fullName);
}